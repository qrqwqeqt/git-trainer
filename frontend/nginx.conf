server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA: усі маршрути віддаємо index.html (стан живе в query-параметрах).
    location / {
        try_files $uri $uri/ /index.html;
    }
}
